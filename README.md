# Emotion-Recognition-System using ( Machine learning + Deep Learning )

In this project we can detect human emotion (happy, sad, angry etc) from their facial expression. We can also detect real time emotion.
